# Audit all gems listed in the Gemfile for known security problems by running bin/bundler-audit.
# If CI job security_gems fails, run: bin/bundler-audit check
# Then add the reported CVE or advisory ID to the ignore list below if you accept the risk.

ignore: []
